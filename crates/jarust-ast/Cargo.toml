[package]
name = "jarust-ast"
description = "Zero-dependency Java AST parser for the jarust migration engine."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
# Minimal zero-dependency parsing logic